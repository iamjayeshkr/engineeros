# Supabase (Auth + Postgres)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Prisma — Supabase connection string (use the "Transaction" pooler URL for DATABASE_URL,
# and the "Session" direct URL for DIRECT_URL if you add it to schema.prisma later)
DATABASE_URL=

# App
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# AI
GEMINI_API_KEY=
